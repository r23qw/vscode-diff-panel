{
  "name": "diff-panel",
  "displayName": "Diff Panel",
  "version": "1.0.0",
  "description": "A quick way to compare two files",
  "author": "Aaron00101010",
  "categories": [
    "Other"
  ],
  "main": "./out/src/extension.js",
  "engines": {
    "vscode": "^1.46.0"
  },
  "activationEvents": [],
  "browser": "./out/src/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "diff-panel.newDiffEditor",
        "title": "Diff Panel: New Diff Editor"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "command": "diff-panel.newDiffEditor",
          "group": "navigation",
          "when": "resourceScheme == diff-panel"
        }
      ]
    },
    "views": {
      "diff-panel": [
        {
          "id": "diff-panel",
          "name": "Diff Panel",
          "type": "webview"
        }
      ]
    },
    "viewsContainers": {
      "activitybar": [
        {
          "icon": "$(diff)",
          "id": "diff-panel",
          "title": "Diff Panel"
        }
      ]
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "dev:webview": "cd webview-ui && pnpm run dev",
    "build:webview": "cd webview-ui && pnpm run build",
    "install:all": "pnpm install && cd webview-ui && pnpm install",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "pretest": "pnpm run compile && pnpm run lint",
    "vscode:prepublish": "pnpm run compile && pnpm run build:webview",
    "watch": "tsc -watch -p ./"
  },
  "dependencies": {
    "@vscode/codicons": "^0.0.32"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^0.38.5",
    "@types/glob": "^7.2.0",
    "@types/node": "^12.20.55",
    "@types/vscode": "^1.77.0",
    "eslint": "^8.39.0",
    "glob": "^7.2.3",
    "lint-staged": "^13.2.1",
    "prettier": "^2.8.7",
    "simple-git-hooks": "^2.8.1",
    "typescript": "^4.9.5",
    "vscode-test": "^1.6.1"
  },
  "simple-git-hooks": {
    "pre-commit": "pnpm lint-staged"
  },
  "lint-staged": {
    "*": "eslint --fix"
  }
}
